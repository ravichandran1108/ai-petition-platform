# Server configuration
server.port=8080
server.servlet.context-path=/api

# Comment out MongoDB configuration since we're using in-memory storage
# spring.data.mongodb.host=localhost
# spring.data.mongodb.port=27017
# spring.data.mongodb.database=petition_db

# Spring configuration
spring.application.name=petition-platform 